import React from 'react'


const Testimonial = (testimonial) => {
    return (
        <>

            {
                testimonial.map((item, map) => {

                })
            }

        </>
    )
}

export default Testimonial